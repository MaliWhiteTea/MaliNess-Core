# 0.1.6.3-alpha.1 — yapılacaklar

> **Sürüm:** `0.1.6.3-alpha.1` (implementasyon `başla` denince `pom.xml`)  
> **Kapsam:** GUI altyapısı + YAML + demo. Sistem entegrasyonu **yok**.

---

## Sınır

- E tuşu envanteri **dahil değil**
- Fullscreen / snapshot **yok**
- Tipler: **CHEST**, **HOPPER**, **DROPPER**
- ItemsAdder, PAPI, pwarp/warp/home bağlama → UNSCHEDULED

---

## Metin

| Kaynak | İçerik |
|--------|--------|
| `guis/<id>.yml` | title, item name, item lore (+ `{page}` placeholder) |
| `langs/gui.yml` | Sistem mesajları only |
| `configs/gui.yml` | Genel ayarlar, logging |

**Lore:** Tek `lore:` listesi; `{target_page}`, `{pages_back}` vb. render’da replace (ayrı `dynamic-lore` bloğu yok).

---

## Mandatory

- ESC → reopen · reload → reopen · restart → kaçış
- Çıkış: `MenuService.release(player, source, detail)` + log
- Kısıtlar **yalnızca mandatory:** `block-movement`, `allow-damage`, `allow-chat`

---

## AFK demo (`demo-mandatory-afk.yml`)

- Tip: **HOPPER** (5 slot)
- Her açılışta: **1× karpuz** (`MELON_SLICE`, doğru slot = `release`) + **4 rastgele vanilla item**, slotlar rastgele
- Yanlış item → menü açık kalır

---

## Demo pwarp

- `DemoPwarpContentProvider`: sahte kafalar, tıklayınca chat mesajı + click türü
- Sort/filter session’da; demo listeyi gerçekten sıralar/filtreler
- Boş liste: content boş menü açılır

---

## Render

1. content → 2. chrome → 3. filler (prev yoksa slot boş veya filler)

---

## Action referansı

Tam liste → `TEST_CHECKLISTS/0.1.6.3.md` § Action referansı

---

## Implementasyon sırası

1. Holder, Session, MenuService (open / close / release)
2. MenuListener — 10 click türü + drag cancel
3. YAML loader + MenuRegistry + render pipeline
4. Pagination + dynamic stack + lore placeholder
5. Mandatory (reopen, movement, chat, command, log, relog session)
6. ContentProvider API + demo pwarp + demo AFK
7. `open-menu:<id>` action
8. `/mngui` + `plugin.yml` permissions
9. Reload hook · `langs/gui.yml` · test checklist doldurma

---

## Demo dosyalar

- `guis/demo-pwarp-layout.yml`
- `guis/demo-pwarp-empty.yml`
- `guis/demo-mandatory-afk.yml`
- `guis/demo-overlay-locked.yml`
- `guis/demo-overlay-allowed.yml`
- `guis/demo-hopper.yml` · `guis/demo-dropper.yml`

---

## Planlanan checklist

- [ ] Tüm maddeler → `TEST_CHECKLISTS/0.1.6.3.md`

---

## Dışarıda

pwarp/warp/home, ItemsAdder, PAPI, onay huni, ses, Folia
