enabled: true

close-on-other-inventory: true
pickup-while-menu-open: true

logging:
  mandatory-release: true

click-protection:
  double-click-guard: true
  button-cooldown:
    enabled: true
    duration-ms: 350

economy-defaults:
  on-insufficient: close
  on-success: close
  on-error: close

defaults:
  player-inventory: locked
  close-policy: normal
  close-on-command: false
  escape-on-death: false
  escape-on-quit: true
  block-movement: true
  allow-damage: true
  allow-chat: false
  filler:
    enabled: false
